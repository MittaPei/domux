# Post-v2 v3 source archive

These two source files preserve the exact policy and regression-test bytes
bound by `../validation.json`. The sibling `../ha_acceptance.json` preserves
the earlier three-case Home Assistant record bound by that same validation.

They were archived when final submission review produced a later, separately
versioned correction-policy and four-case HA acceptance closure. The v3 record,
its 106 policy tests, 184 full-case tests, and its hashes remain unchanged.
